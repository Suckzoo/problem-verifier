# ICPC Problem Verifier — 계획 3: GitHub Actions 통합 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 문제 repo가 `uses: suckzoo/problem-verifier/.github/workflows/verify.yml@<ref>` 한 줄로 solution 병렬 채점과 리포트를 받게 한다.

**Architecture:** CLI에 `discover`/`report` 하위 명령을 추가하고, composite action 3개(discover/judge/report)는 그 CLI의 얇은 YAML wrapper로 만든다. reusable workflow가 `discover → judge (matrix, solution별 1 job) → report`를 엮는다. 변경 감지와 리포트 생성은 순수 python이라 로컬 단위 테스트로 검증하고, action/workflow는 이 repo의 selftest workflow가 fixture 문제로 end-to-end 검증한다.

**Tech Stack:** 계획 1·2와 동일 + GitHub Actions (composite actions, reusable workflow, upload/download-artifact v4).

## Global Constraints

계획 1·2의 Global Constraints가 그대로 적용된다. 추가:

- 변경 감지 규칙 (spec §5): `full` input, `workflow_dispatch`/`schedule` 이벤트, 문제 전역 파일(`problem.yaml`, `data/**`, `output_validator*/**`, `include/**`) 변경 시 전체 재채점. 그 외 push/pull_request는 solution diff만. 변경 목록을 계산할 수 없으면(신규 branch, shallow, git 실패) 전체로 fallback.
- matrix 항목은 `{name, path, expected, lang}`이고 `name`은 `[^A-Za-z0-9._-]`를 `_`로 바꾼 값 (기존 `solutions.sanitize_name`과 동일 규칙).
- Job Summary는 GitHub이 1 MiB에서 자른다: testcase 200개 초과 시 띠 대신 요약(verdict별 개수 + 실패 id 최대 50개), 런타임 표는 `accepted` 기대 또는 기대 불일치 solution에만·행 최대 100개, 전체 markdown 900 KiB 초과 시 뒤쪽 solution 섹션부터 잘라내고 HTML artifact 안내를 남긴다.
- verdict 기호: 🟩 AC / 🟥 WA / 🟨 TLE / 🟧 RTE / 🟪 CE·JE / ⬜ 미실행 (`verdicts.SUMMARY_ICON` 재사용).
- 권장 시간제한 = `ceil((가장 느린 AC wall / 그 job의 machine_factor) * time_multiplier * 10) / 10`. 판정에는 쓰지 않고 리포트에만 표시.
- HTML 리포트는 단일 파일, 외부 의존 없음. **validator/judgemessage와 출력 excerpt는 전부 `html.escape`를 거친다** (계획 2 최종 리뷰: judgemessage는 신뢰할 수 없는 텍스트다).
- diff excerpt: 실패 testcase 중 앞 `diff-max-cases`(기본 3)개만, expected/actual 각각 `diff-max-bytes`(기본 4096) byte 상한.
- report job은 기대 불일치, `judge_error`/`compiler_error`(기대가 그것이 아닐 때), 결과 artifact 누락 중 하나라도 있으면 실패한다.
- action은 사용자 repo에서 돌므로 icpc_verify 패키지를 스스로 설치해야 한다: 각 action이 `${{ github.action_path }}/..`를 pip install한다. reusable workflow는 `github.workflow_ref`에서 자기 ref를 파싱해 같은 ref의 action repo를 `.icpc-verifier/`에 checkout한다.
- judge action은 ghcr pull 전에 `docker login ghcr.io`를 `github.actor`/`github.token`으로 수행한다 (private package 대응; public이어도 무해).
- 이 호스트는 arm64 macOS다: python 단위 테스트는 로컬, action/workflow는 branch push 후 CI + selftest workflow로 검증한다.
- mainline 단위 테스트는 현재 155개다. 각 task는 이를 깨지 않는다.

## 실행 순서 (병렬성)

- Task 1(discover)과 Task 2(diff excerpt)는 **병렬 가능** (cli.py 충돌은 controller가 merge에서 해소).
- Task 3(report)은 Task 2의 result.json 확장에 의존한다. Task 4는 1·3에, Task 5는 4에 의존한다.

---

### Task 1: 변경 감지와 discover CLI

**Files:**
- Create: `src/icpc_verify/discover.py`
- Modify: `src/icpc_verify/cli.py` (discover 하위 명령 추가)
- Test: `tests/unit/test_discover.py`

**Interfaces:**
- Consumes: `solutions.discover_solutions`, `solutions.Solution`, `problemcfg.load_problem_config`, `testdata.collect_testcases`(검증용)
- Produces:
  - `discover.decide_scope(*, full_flag: bool, event_name: str, changed_files: list[str] | None, problem_dir_rel: str) -> tuple[bool, str]` — `(전체 재채점 여부, 사유 문자열)`. 순수 함수.
  - `discover.changed_solution_units(changed_files: list[str], problem_dir_rel: str) -> set[str]` — 변경 파일을 solution 단위 rel_path(`accepted/a.cpp` 또는 `accepted/multi`)로 사상. 순수 함수.
  - `discover.build_matrix(solutions: list[Solution], *, full: bool, changed_units: set[str]) -> list[dict]` — `{name, path, expected, lang}` 목록. `path`는 solution rel_path.
  - CLI `icpc-verify discover --problem-dir D --output out.json [--full] [--event-name E] [--before SHA] [--head SHA] [--base-ref R] [--solutions-filter G] [--default-time-limit F] [--default-memory-mib N]`
    - git diff는 CLI가 수행한다: push면 `git diff --name-only <before> <head>`, pull_request면 `git merge-base <base-ref> HEAD` 후 diff. 실패하거나 before가 전부 0이면 `changed_files=None`.
    - out.json: `{"matrix": [...], "count": N, "full": bool, "reason": str, "problem": {"name", "time_limit", "memory_mib", "time_multiplier", "validation"}}`
    - exit: 정상 0, 설정 오류(계획 1과 같은 예외들) 2. **count가 0이어도 0으로 끝난다** (workflow가 skip 처리).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/unit/test_discover.py`:

```python
import json
import subprocess
import sys
from pathlib import Path

import pytest

from icpc_verify.discover import build_matrix, changed_solution_units, decide_scope
from icpc_verify.solutions import discover_solutions


def test_full_flag_wins():
    full, reason = decide_scope(
        full_flag=True, event_name="push", changed_files=["solutions/accepted/a.cpp"],
        problem_dir_rel="",
    )
    assert full
    assert "full" in reason


@pytest.mark.parametrize("event", ["workflow_dispatch", "schedule"])
def test_manual_events_are_full(event):
    full, _ = decide_scope(
        full_flag=False, event_name=event, changed_files=[], problem_dir_rel="",
    )
    assert full


def test_unknown_changes_fall_back_to_full():
    full, reason = decide_scope(
        full_flag=False, event_name="push", changed_files=None, problem_dir_rel="",
    )
    assert full
    assert "diff" in reason


@pytest.mark.parametrize(
    "path",
    [
        "problem.yaml",
        "data/01.in",
        "data/secret/03.ans",
        "output_validators/check/check.cpp",
        "output_validator/run",
        "include/util.h",
    ],
)
def test_problem_global_files_trigger_full(path):
    full, reason = decide_scope(
        full_flag=False, event_name="push", changed_files=[path], problem_dir_rel="",
    )
    assert full
    assert path.split("/")[0].rstrip("s") in reason or "problem" in reason


def test_solution_only_changes_are_incremental():
    full, _ = decide_scope(
        full_flag=False, event_name="push",
        changed_files=["solutions/accepted/a.cpp", "README.md"],
        problem_dir_rel="",
    )
    assert not full


def test_problem_dir_prefix_is_respected():
    # 문제 package 가 repo 하위 디렉토리(prob/)에 있을 때, 다른 문제의 변경은 무시한다
    full, _ = decide_scope(
        full_flag=False, event_name="push",
        changed_files=["other/problem.yaml"], problem_dir_rel="prob",
    )
    assert not full
    full, _ = decide_scope(
        full_flag=False, event_name="push",
        changed_files=["prob/problem.yaml"], problem_dir_rel="prob",
    )
    assert full


def test_changed_solution_units_maps_files_to_units():
    units = changed_solution_units(
        [
            "solutions/accepted/a.cpp",
            "solutions/accepted/multi/b.cpp",
            "solutions/wrong_answer/x.py",
            "solutions/accepted/multi/nested/c.h",
            "README.md",
        ],
        "",
    )
    assert units == {"accepted/a.cpp", "accepted/multi", "wrong_answer/x.py"}


def test_changed_solution_units_with_problem_dir():
    units = changed_solution_units(["prob/solutions/accepted/a.cpp"], "prob")
    assert units == {"accepted/a.cpp"}


def make_solutions(tmp_path):
    for rel, text in {
        "accepted/a.cpp": "int main(){}",
        "wrong_answer/b.cpp": "int main(){}",
    }.items():
        p = tmp_path / "solutions" / rel
        p.parent.mkdir(parents=True, exist_ok=True)
        p.write_text(text, encoding="utf-8")
    sols, _ = discover_solutions(tmp_path)
    return sols


def test_build_matrix_full(tmp_path):
    entries = build_matrix(make_solutions(tmp_path), full=True, changed_units=set())
    assert [e["path"] for e in entries] == ["accepted/a.cpp", "wrong_answer/b.cpp"]
    assert entries[0] == {
        "name": "accepted_a.cpp", "path": "accepted/a.cpp",
        "expected": "accepted", "lang": "cpp",
    }


def test_build_matrix_incremental(tmp_path):
    entries = build_matrix(
        make_solutions(tmp_path), full=False, changed_units={"wrong_answer/b.cpp"}
    )
    assert [e["path"] for e in entries] == ["wrong_answer/b.cpp"]


FIXTURE = Path(__file__).resolve().parents[2] / "tests" / "fixtures" / "plain"


def run_cli(tmp_path, *extra):
    out = tmp_path / "discover.json"
    proc = subprocess.run(
        [
            sys.executable, "-m", "icpc_verify.cli", "discover",
            "--problem-dir", str(FIXTURE),
            "--output", str(out),
            *extra,
        ],
        capture_output=True, text=True,
    )
    payload = json.loads(out.read_text(encoding="utf-8")) if out.is_file() else None
    return proc, payload


def test_cli_full_discovers_fixture(tmp_path):
    proc, payload = run_cli(tmp_path, "--full")
    assert proc.returncode == 0, proc.stderr
    assert payload["full"] is True
    assert payload["count"] == 5
    assert payload["problem"]["name"] == "Add Two Numbers"
    assert payload["problem"]["time_limit"] == 1.0
    assert payload["problem"]["validation"] == "default"
    names = {e["name"] for e in payload["matrix"]}
    assert "accepted_main.cpp" in names


def test_cli_without_git_context_falls_back_to_full(tmp_path):
    # --before/--head 없이 push 이벤트면 diff 를 계산할 수 없어 전체가 된다
    proc, payload = run_cli(tmp_path, "--event-name", "push")
    assert proc.returncode == 0
    assert payload["full"] is True


def test_cli_missing_problem_yaml_exits_two(tmp_path):
    out = tmp_path / "d.json"
    proc = subprocess.run(
        [
            sys.executable, "-m", "icpc_verify.cli", "discover",
            "--problem-dir", str(tmp_path),
            "--output", str(out),
        ],
        capture_output=True, text=True,
    )
    assert proc.returncode == 2
    assert "problem.yaml" in proc.stderr
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `PYTHONPATH=src python3 -m pytest tests/unit/test_discover.py -v`
Expected: FAIL. `ModuleNotFoundError: No module named 'icpc_verify.discover'`

- [ ] **Step 3: discover.py를 구현한다**

`src/icpc_verify/discover.py`:

```python
"""무엇을 채점할지 결정한다 (spec §5).

순수 함수만 둔다. git 호출과 이벤트 파싱은 cli.py 가 한다.
"""

from __future__ import annotations

from .solutions import Solution

FULL_EVENTS = ("workflow_dispatch", "schedule")
PROBLEM_GLOBAL_PREFIXES = ("data/", "output_validators/", "output_validator/", "include/")


def _relativize(path: str, problem_dir_rel: str) -> str | None:
    """repo 기준 경로를 문제 디렉토리 기준으로 바꾼다. 문제 밖이면 None."""
    if not problem_dir_rel or problem_dir_rel == ".":
        return path
    prefix = problem_dir_rel.rstrip("/") + "/"
    if path.startswith(prefix):
        return path[len(prefix):]
    return None


def decide_scope(
    *,
    full_flag: bool,
    event_name: str,
    changed_files: list[str] | None,
    problem_dir_rel: str,
) -> tuple[bool, str]:
    if full_flag:
        return True, "full input 이 켜져 있습니다"
    if event_name in FULL_EVENTS:
        return True, f"{event_name} 이벤트는 전체를 재채점합니다"
    if changed_files is None:
        return True, "변경 목록(diff)을 계산할 수 없어 전체로 fallback 합니다"

    for path in changed_files:
        rel = _relativize(path, problem_dir_rel)
        if rel is None:
            continue
        if rel == "problem.yaml" or rel.startswith(PROBLEM_GLOBAL_PREFIXES):
            return True, f"문제 전역 파일이 바뀌었습니다: {path}"
    return False, "solution 변경만 감지되었습니다"


def changed_solution_units(changed_files: list[str], problem_dir_rel: str) -> set[str]:
    """변경 파일을 solution 단위 rel_path 로 사상한다.

    solutions/<verdict>/<file> 은 그 파일이, solutions/<verdict>/<dir>/... 은
    첫 단계 디렉토리가 단위다.
    """
    units: set[str] = set()
    for path in changed_files:
        rel = _relativize(path, problem_dir_rel)
        if rel is None or not rel.startswith("solutions/"):
            continue
        parts = rel.split("/")
        if len(parts) < 3:
            continue
        if len(parts) == 3:
            units.add(f"{parts[1]}/{parts[2]}")
        else:
            units.add(f"{parts[1]}/{parts[2]}")
    return units


def build_matrix(
    solutions: list[Solution], *, full: bool, changed_units: set[str]
) -> list[dict]:
    entries: list[dict] = []
    for solution in solutions:
        if not full and solution.rel_path not in changed_units:
            continue
        entries.append(
            {
                "name": solution.name,
                "path": solution.rel_path,
                "expected": solution.expected,
                "lang": solution.language.value if solution.language else "",
            }
        )
    return entries
```

주의: `changed_solution_units`의 `len(parts) == 3` 분기와 else 분기가 지금 같은 값을
넣는다 — 파일이든 디렉토리 하위든 단위는 `parts[1]/parts[2]`다. 한 줄로 합쳐서 구현해도
된다 (테스트가 요구하는 것은 결과뿐이다).

- [ ] **Step 4: cli.py에 discover 하위 명령을 추가한다**

`build_parser`에 추가:

```python
    discover = sub.add_parser("discover", help="채점 대상을 결정한다")
    discover.add_argument("--problem-dir", type=Path, default=Path("."))
    discover.add_argument("--output", type=Path, required=True)
    discover.add_argument("--full", action="store_true")
    discover.add_argument("--event-name", default="")
    discover.add_argument("--before", default="")
    discover.add_argument("--head", default="")
    discover.add_argument("--base-ref", default="")
    discover.add_argument("--solutions-filter", default="")
    discover.add_argument("--default-time-limit", type=float, default=1.0)
    discover.add_argument("--default-memory-mib", type=int, default=2048)
```

실행 함수:

```python
def _git_changed_files(args: argparse.Namespace) -> list[str] | None:
    """diff 를 계산한다. 못 하면 None (전체 fallback)."""

    def diff(base: str, head: str) -> list[str] | None:
        proc = subprocess.run(
            ["git", "diff", "--name-only", base, head],
            capture_output=True, text=True, cwd=args.problem_dir,
        )
        if proc.returncode != 0:
            return None
        return [line for line in proc.stdout.splitlines() if line]

    if args.event_name == "pull_request" and args.base_ref:
        merge_base = subprocess.run(
            ["git", "merge-base", f"origin/{args.base_ref}", "HEAD"],
            capture_output=True, text=True, cwd=args.problem_dir,
        )
        if merge_base.returncode != 0:
            return None
        return diff(merge_base.stdout.strip(), "HEAD")

    if args.event_name == "push" and args.before and args.head:
        if set(args.before) == {"0"}:
            return None
        return diff(args.before, args.head)

    return None


def run_discover(args: argparse.Namespace) -> int:
    problem_dir = args.problem_dir.resolve()
    config = load_problem_config(
        problem_dir,
        default_time_limit=args.default_time_limit,
        default_memory_mib=args.default_memory_mib,
    )
    collect_testcases(problem_dir)  # data/ 짝 검사를 discover 단계에서 수행한다

    repo_root = subprocess.run(
        ["git", "rev-parse", "--show-toplevel"],
        capture_output=True, text=True, cwd=problem_dir,
    )
    if repo_root.returncode == 0:
        problem_dir_rel = str(
            problem_dir.relative_to(Path(repo_root.stdout.strip()))
        ).replace("\\", "/")
        if problem_dir_rel == ".":
            problem_dir_rel = ""
    else:
        problem_dir_rel = ""

    changed = _git_changed_files(args)
    full, reason = decide_scope(
        full_flag=args.full,
        event_name=args.event_name,
        changed_files=changed,
        problem_dir_rel=problem_dir_rel,
    )
    changed_units = (
        changed_solution_units(changed, problem_dir_rel) if changed else set()
    )

    solutions, warnings = discover_solutions(
        problem_dir, filter_glob=args.solutions_filter
    )
    matrix = build_matrix(solutions, full=full, changed_units=changed_units)

    payload = {
        "matrix": matrix,
        "count": len(matrix),
        "full": full,
        "reason": reason,
        "problem": {
            "name": config.name,
            "time_limit": config.time_limit,
            "memory_mib": config.memory_mib,
            "time_multiplier": config.time_multiplier,
            "validation": config.validation.value,
        },
    }
    args.output.parent.mkdir(parents=True, exist_ok=True)
    args.output.write_text(json.dumps(payload, ensure_ascii=False, indent=2), encoding="utf-8")

    for warning in warnings:
        print(f"warning: {warning}", file=sys.stderr)
    print(f"discover: {len(matrix)}개 대상 ({reason})")
    return EXIT_OK
```

`main`의 dispatch에 `discover` 분기를 추가하고 `import subprocess`를 넣는다.
`run_discover`는 기존 예외 처리(config error → 2)를 그대로 탄다.

- [ ] **Step 5: 테스트가 통과하는지 확인한다**

Run: `PYTHONPATH=src python3 -m pytest tests/unit/test_discover.py -v`
Expected: PASS. 15개 전부

- [ ] **Step 6: 전체 단위 테스트와 lint**

Run: `ruff check . && ruff format --check . && PYTHONPATH=src python3 -m pytest tests/unit -q`
Expected: 전부 PASS (155 + 15 = 170)

- [ ] **Step 7: 커밋**

```bash
git add src/icpc_verify/discover.py src/icpc_verify/cli.py tests/unit/test_discover.py
git commit -m "feat: add discover command with change detection"
```

---

### Task 2: 실패 testcase의 diff excerpt

**Files:**
- Modify: `src/icpc_verify/results.py` (`TestCaseResult`에 필드 2개 추가)
- Modify: `src/icpc_verify/judge.py` (excerpt 채우기)
- Modify: `src/icpc_verify/cli.py` (judge에 `--diff-max-cases`, `--diff-max-bytes`)
- Test: `tests/unit/test_diff_excerpts.py`

**Interfaces:**
- Consumes: 기존 `TestCaseResult`, `JudgeOptions`, `judge_solution`의 default-validation 경로
- Produces:
  - `TestCaseResult.expected_excerpt: str = ""`, `TestCaseResult.actual_excerpt: str = ""`
  - `JudgeOptions.diff_max_cases: int = 3`, `JudgeOptions.diff_max_bytes: int = 4096`
  - `judge._make_excerpt(data: bytes, cap: int) -> str` — 앞 `cap` byte를 `errors="replace"`로 decode. 잘렸으면 끝에 `"\n… (잘림)"`을 붙인다.
  - 규칙: **default validation에서 verdict이 `wrong_answer`인 testcase**에 한해, solution당 앞에서 `diff_max_cases`개까지 `expected_excerpt`/`actual_excerpt`를 채운다. custom/interactive는 채우지 않는다 (judgemessage가 그 역할).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/unit/test_diff_excerpts.py`:

```python
from icpc_verify.judge import _make_excerpt
from icpc_verify.results import TestCaseResult


def test_excerpt_fields_default_empty():
    case = TestCaseResult(
        id="01", group="", verdict="accepted", wall=0.1, cpu=0.1,
        mem_kib=1, exit_code=0, message="",
    )
    assert case.expected_excerpt == ""
    assert case.actual_excerpt == ""


def test_make_excerpt_short_passthrough():
    assert _make_excerpt(b"1 2 3\n", 100) == "1 2 3\n"


def test_make_excerpt_caps_and_marks():
    text = _make_excerpt(b"x" * 50, 10)
    assert text.startswith("x" * 10)
    assert text.endswith("(잘림)")


def test_make_excerpt_tolerates_invalid_utf8():
    text = _make_excerpt(b"\xff\xfeok", 100)
    assert "ok" in text
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `PYTHONPATH=src python3 -m pytest tests/unit/test_diff_excerpts.py -v`
Expected: FAIL (`expected_excerpt` 필드 없음, `_make_excerpt` 없음)

- [ ] **Step 3: 구현한다**

`results.py`의 `TestCaseResult`에 추가:

```python
    expected_excerpt: str = ""
    actual_excerpt: str = ""
```

`judge.py`:

```python
EXCERPT_TRUNCATION_MARK = "\n… (잘림)"


def _make_excerpt(data: bytes, cap: int) -> str:
    text = data[:cap].decode("utf-8", errors="replace")
    if len(data) > cap:
        text += EXCERPT_TRUNCATION_MARK
    return text
```

`JudgeOptions`에 `diff_max_cases: int = 3`, `diff_max_bytes: int = 4096`을 추가한다.

`judge_solution`의 default-validation 분기에서, verdict이 `wrong_answer`로 결정된 직후
solution별 카운터(`excerpts_taken`)가 `options.diff_max_cases` 미만이면:

```python
            if verdict == verdicts.WRONG_ANSWER and excerpts_taken < options.diff_max_cases:
                excerpts_taken += 1
                case_result.expected_excerpt = _make_excerpt(
                    case.answer_path.read_bytes(), options.diff_max_bytes
                )
                case_result.actual_excerpt = _make_excerpt(
                    team_output, options.diff_max_bytes
                )
```

(`case_result`는 append 전의 `TestCaseResult` 인스턴스. 현재 코드가 append 시점에 생성한다면
생성 후 필드를 채우고 append하도록 바꾼다. custom/interactive 분기에는 손대지 않는다.)

`cli.py`의 judge 인자에 추가하고 `JudgeOptions`로 전달한다:

```python
    judge.add_argument("--diff-max-cases", type=int, default=3)
    judge.add_argument("--diff-max-bytes", type=int, default=4096)
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `PYTHONPATH=src python3 -m pytest tests/unit/test_diff_excerpts.py tests/unit -q`
Expected: 전부 PASS

- [ ] **Step 5: docker 테스트를 하나 추가한다**

`tests/docker/test_judge.py`에 추가 (CI에서 검증):

```python
def test_wrong_answer_carries_diff_excerpts(tmp_path):
    result = judge(tmp_path, "wrong_answer/off_by_one.cpp")
    first = result.testcases[0]
    assert first.verdict == verdicts.WRONG_ANSWER
    assert first.expected_excerpt.strip() == "3"
    assert first.actual_excerpt.strip() == "4"
```

- [ ] **Step 6: 커밋**

```bash
git add src/icpc_verify/results.py src/icpc_verify/judge.py src/icpc_verify/cli.py \
  tests/unit/test_diff_excerpts.py tests/docker/test_judge.py
git commit -m "feat: capture expected/actual excerpts for wrong answers"
```

---

### Task 3: 리포트 생성기와 report CLI

**Files:**
- Create: `src/icpc_verify/report.py`
- Modify: `src/icpc_verify/cli.py` (report 하위 명령)
- Test: `tests/unit/test_report.py`

**Interfaces:**
- Consumes: Task 2까지의 result.json 구조 (`SolutionResult`의 `dataclasses.asdict` + `expectation_met`/`time_limit`/`hard_kill`), discover의 `problem` dict, matrix 항목 `{name, path, expected, lang}`
- Produces:
  - `report.Report` dataclass: `markdown: str`, `html: str`, `passed: bool`, `failures: list[str]`
  - `report.generate_report(results: list[dict], problem: dict, *, expected_matrix: list[dict] | None, verdict_match: str) -> Report`
  - CLI `icpc-verify report --results-dir D --problem P.json --summary-out S.md --html-out H.html [--expected-matrix M.json] [--verdict-match exact]`
    - `--results-dir` 아래에서 `**/result*.json`을 재귀 수집한다.
    - exit 0 = passed, 1 = failures 존재, 2 = 설정 오류.

규칙 (Global Constraints 재확인): 요약 표 → solution 섹션(띠 20개/줄) → 런타임 표(조건부) →
권장 시간제한 → 크기 상한 3종 → 누락 검출 → HTML(matrix, 바 차트, diff, judgemessage escape).

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/unit/test_report.py`:

```python
import json

from icpc_verify.report import generate_report

PROBLEM = {
    "name": "Add Two Numbers", "time_limit": 1.0, "memory_mib": 512,
    "time_multiplier": 5.0, "validation": "default",
}


def case(id="01", verdict="accepted", wall=0.1, **kwargs):
    base = {
        "id": id, "group": "", "verdict": verdict, "wall": wall, "cpu": wall,
        "mem_kib": 1000, "exit_code": 0, "message": "",
        "expected_excerpt": "", "actual_excerpt": "",
    }
    base.update(kwargs)
    return base


def result(rel_path="accepted/main.cpp", expected="accepted", verdict="accepted",
           testcases=None, machine_factor=1.0, expectation_met=True, **kwargs):
    base = {
        "name": rel_path.replace("/", "_"), "rel_path": rel_path,
        "expected": expected, "language": "cpp", "verdict": verdict,
        "testcases": testcases if testcases is not None else [case()],
        "compile_log": "", "machine_factor": machine_factor,
        "cpu_isolated": True, "warnings": [],
        "expectation_met": expectation_met, "time_limit": 1.0, "hard_kill": 3.0,
    }
    base.update(kwargs)
    return base


def test_all_green_passes():
    r = generate_report([result()], PROBLEM, expected_matrix=None, verdict_match="exact")
    assert r.passed
    assert r.failures == []
    assert "Add Two Numbers" in r.markdown
    assert "🟩" in r.markdown


def test_mismatch_fails_and_is_listed():
    bad = result(rel_path="time_limit_exceeded/fast.cpp", expected="time_limit_exceeded",
                 verdict="accepted", expectation_met=False)
    r = generate_report([bad], PROBLEM, expected_matrix=None, verdict_match="exact")
    assert not r.passed
    assert any("time_limit_exceeded/fast.cpp" in f for f in r.failures)
    assert "❌" in r.markdown


def test_missing_result_fails():
    matrix = [
        {"name": "accepted_main.cpp", "path": "accepted/main.cpp",
         "expected": "accepted", "lang": "cpp"},
        {"name": "accepted_gone.py", "path": "accepted/gone.py",
         "expected": "accepted", "lang": "python"},
    ]
    r = generate_report([result()], PROBLEM, expected_matrix=matrix, verdict_match="exact")
    assert not r.passed
    assert any("accepted/gone.py" in f for f in r.failures)
    assert "미실행" in r.markdown or "누락" in r.markdown


def test_not_run_uses_white_square():
    cases = [case("01", "wrong_answer"), case("02", "not_run", wall=0.0)]
    bad = result(rel_path="wrong_answer/w.cpp", expected="wrong_answer",
                 verdict="wrong_answer", testcases=cases)
    r = generate_report([bad], PROBLEM, expected_matrix=None, verdict_match="exact")
    assert "🟥⬜" in r.markdown.replace("\n", "")


def test_stripes_wrap_at_20():
    cases = [case(f"{i:02d}") for i in range(1, 46)]
    r = generate_report(
        [result(testcases=cases)], PROBLEM, expected_matrix=None, verdict_match="exact"
    )
    lines = [ln for ln in r.markdown.splitlines() if ln and set(ln) <= {"🟩"}]
    assert [len(ln) for ln in lines] == [20, 20, 5]


def test_over_200_cases_collapses_to_summary():
    cases = [case(f"{i:03d}") for i in range(1, 202)]
    cases[100]["verdict"] = "wrong_answer"
    r = generate_report(
        [result(verdict="wrong_answer", expected="wrong_answer", testcases=cases)],
        PROBLEM, expected_matrix=None, verdict_match="exact",
    )
    assert "🟩🟩🟩" not in r.markdown          # 띠 없음
    assert "201" in r.markdown                 # 개수 요약
    assert "101" in r.markdown                 # 실패 id 목록


def test_suggested_time_limit():
    fast = result(testcases=[case(wall=0.2)], machine_factor=1.0)
    slow = result(rel_path="accepted/slow.py", language="python",
                  testcases=[case(wall=0.42)], machine_factor=2.0)
    r = generate_report([fast, slow], PROBLEM, expected_matrix=None, verdict_match="exact")
    # 보정 최저치: max(0.2/1.0, 0.42/2.0)=0.21 -> 0.21*5=1.05 -> ceil(10.5)/10=1.1
    assert "1.1" in r.markdown


def test_markdown_capped_under_900kib():
    cases = [case(f"{i:03d}", message="x" * 3000) for i in range(1, 150)]
    results = [
        result(rel_path=f"accepted/s{k}.cpp", testcases=list(cases)) for k in range(40)
    ]
    r = generate_report(results, PROBLEM, expected_matrix=None, verdict_match="exact")
    assert len(r.markdown.encode()) <= 900 * 1024
    assert "HTML" in r.markdown                # 잘림 안내


def test_html_is_selfcontained_and_escapes():
    evil = result(
        rel_path="wrong_answer/evil.cpp", expected="wrong_answer",
        verdict="wrong_answer",
        testcases=[case(verdict="wrong_answer",
                        message="<script>alert(1)</script>",
                        expected_excerpt="<b>2 3</b>", actual_excerpt="1 & 4")],
    )
    r = generate_report([evil], PROBLEM, expected_matrix=None, verdict_match="exact")
    assert "<script>alert(1)" not in r.html
    assert "&lt;script&gt;" in r.html
    assert "&lt;b&gt;2 3&lt;/b&gt;" in r.html
    assert "http" not in r.html.lower() or "://" not in r.html  # 외부 참조 없음


def test_html_contains_runtime_bars():
    r = generate_report(
        [result(testcases=[case(wall=0.5)])], PROBLEM,
        expected_matrix=None, verdict_match="exact",
    )
    assert "time-bar" in r.html


def test_machine_factor_warning():
    r = generate_report(
        [result(machine_factor=1.5)], PROBLEM, expected_matrix=None, verdict_match="exact"
    )
    assert "1.50" in r.markdown
    assert "⚠" in r.markdown
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `PYTHONPATH=src python3 -m pytest tests/unit/test_report.py -v`
Expected: FAIL. `ModuleNotFoundError`

- [ ] **Step 3: report.py를 구현한다**

`src/icpc_verify/report.py`:

```python
"""result.json 들을 병합해 Job Summary(markdown)와 HTML 리포트를 만든다 (spec §11)."""

from __future__ import annotations

import html
import math
from dataclasses import dataclass

from . import verdicts

STRIPE_WIDTH = 20
MAX_STRIPE_CASES = 200
MAX_FAIL_IDS = 50
MAX_TABLE_ROWS = 100
MARKDOWN_CAP = 900 * 1024
MACHINE_FACTOR_WARN = 1.3


@dataclass(frozen=True)
class Report:
    markdown: str
    html: str
    passed: bool
    failures: list[str]


def _icon(verdict: str) -> str:
    return verdicts.SUMMARY_ICON.get(verdict, "⬜")


def _stripes(cases: list[dict]) -> str:
    icons = [_icon(c["verdict"]) for c in cases]
    lines = [
        "".join(icons[i : i + STRIPE_WIDTH]) for i in range(0, len(icons), STRIPE_WIDTH)
    ]
    return "\n".join(lines)


def _case_summary(cases: list[dict]) -> str:
    counts: dict[str, int] = {}
    for c in cases:
        counts[c["verdict"]] = counts.get(c["verdict"], 0) + 1
    parts = [f"{_icon(v)} {v}: {n}" for v, n in sorted(counts.items())]
    failed = [c["id"] for c in cases if c["verdict"] not in (verdicts.ACCEPTED, verdicts.NOT_RUN)]
    text = f"testcase {len(cases)}개 — " + ", ".join(parts)
    if failed:
        shown = failed[:MAX_FAIL_IDS]
        more = f" 외 {len(failed) - len(shown)}개" if len(failed) > len(shown) else ""
        text += f"\n실패: {', '.join(shown)}{more}"
    return text


def _suggested_limit(results: list[dict], multiplier: float) -> float | None:
    slowest = 0.0
    for r in results:
        if r["expected"] != verdicts.ACCEPTED or r["verdict"] != verdicts.ACCEPTED:
            continue
        factor = r.get("machine_factor") or 1.0
        for c in r["testcases"]:
            if c["verdict"] == verdicts.ACCEPTED:
                slowest = max(slowest, c["wall"] / factor)
    if slowest <= 0.0:
        return None
    return math.ceil(slowest * multiplier * 10) / 10


def _runtime_table(cases: list[dict], time_limit: float) -> str:
    rows = ["| tc | verdict | wall | cpu | mem |", "|---|---|---|---|---|"]
    for c in cases[:MAX_TABLE_ROWS]:
        if c["verdict"] == verdicts.NOT_RUN:
            continue
        mem = f"{c['mem_kib'] // 1024}MB" if c["mem_kib"] else "-"
        rows.append(
            f"| {c['id']} | {_icon(c['verdict'])} {c['verdict']} "
            f"| {c['wall']:.3f} | {c['cpu']:.3f} | {mem} |"
        )
    if len(cases) > MAX_TABLE_ROWS:
        rows.append(f"| … | {len(cases) - MAX_TABLE_ROWS}개 생략 | | | |")
    return "\n".join(rows)


def _solution_section(r: dict) -> str:
    ok = r.get("expectation_met", False)
    mark = "✅" if ok else "❌"
    max_wall = max((c["wall"] for c in r["testcases"]), default=0.0)
    limit = r.get("time_limit", 0.0) or 0.0
    pct = f" ({max_wall / limit * 100:.0f}%)" if limit else ""
    factor = r.get("machine_factor", 1.0)
    factor_note = f" · factor {factor:.2f}" + (" ⚠" if factor > MACHINE_FACTOR_WARN else "")

    lines = [
        f"## {mark} {r['rel_path']}",
        f"{r['language']} · 기대 {r['expected']} · 실제 {r['verdict']}"
        f" · max {max_wall:.3f}s{pct}{factor_note}",
        "",
    ]
    cases = r["testcases"]
    if len(cases) > MAX_STRIPE_CASES:
        lines.append(_case_summary(cases))
    else:
        lines.append(_stripes(cases))
    if r["expected"] == verdicts.ACCEPTED or not ok:
        lines += ["", _runtime_table(cases, limit)]
    if r.get("compile_log") and r["verdict"] in (verdicts.COMPILER_ERROR, verdicts.JUDGE_ERROR):
        log = r["compile_log"][:4096]
        lines += ["", "```", log, "```"]
    first_message = next(
        (c["message"] for c in cases if c["message"] and c["verdict"] != verdicts.ACCEPTED),
        "",
    )
    if first_message:
        lines += ["", f"> {first_message[:500].replace(chr(10), ' / ')}"]
    return "\n".join(lines)


def _overview_table(results: list[dict]) -> str:
    rows = [
        "| solution | 언어 | 기대 | 실제 | max wall | factor |",
        "|---|---|---|---|---|---|",
    ]
    for r in results:
        mark = "✅" if r.get("expectation_met") else "❌"
        max_wall = max((c["wall"] for c in r["testcases"]), default=0.0)
        factor = r.get("machine_factor", 1.0)
        warn = " ⚠" if factor > MACHINE_FACTOR_WARN else ""
        rows.append(
            f"| {r['rel_path']} | {r['language']} | {r['expected']} "
            f"| {mark} {r['verdict']} | {max_wall:.3f}s | {factor:.2f}{warn} |"
        )
    return "\n".join(rows)


def generate_report(
    results: list[dict],
    problem: dict,
    *,
    expected_matrix: list[dict] | None,
    verdict_match: str,
) -> Report:
    results = sorted(results, key=lambda r: r["rel_path"])
    failures: list[str] = []
    for r in results:
        if not r.get("expectation_met"):
            failures.append(
                f"{r['rel_path']}: 기대 {r['expected']}, 실제 {r['verdict']}"
            )

    missing: list[dict] = []
    if expected_matrix:
        seen = {r["rel_path"] for r in results}
        for entry in expected_matrix:
            if entry["path"] not in seen:
                missing.append(entry)
                failures.append(f"{entry['path']}: 결과 누락 (judge job 미완료)")

    header = [
        "# ICPC problem verify",
        "",
        f"문제: {problem['name']} · 시간제한 {problem['time_limit']:.3f}s"
        f" · 메모리 {problem['memory_mib']}MiB · validation {problem['validation']}",
    ]
    suggested = _suggested_limit(results, problem.get("time_multiplier", 5.0))
    if suggested is not None:
        header.append(
            f"권장 시간제한: **{suggested:.1f}s**"
            f" (가장 느린 accepted 기준, multiplier {problem.get('time_multiplier', 5.0):g})"
        )
    if missing:
        header.append(f"⚠ 결과 누락(미실행): {', '.join(e['path'] for e in missing)}")
    header += ["", _overview_table(results), ""]

    sections = [_solution_section(r) for r in results]
    markdown = "\n".join(header) + "\n" + "\n\n".join(sections)

    if len(markdown.encode()) > MARKDOWN_CAP:
        kept: list[str] = []
        size = len("\n".join(header).encode()) + 200
        for section in sections:
            size += len(section.encode()) + 2
            if size > MARKDOWN_CAP:
                kept.append("\n_(길이 제한으로 이하 생략 — HTML artifact 를 보세요)_")
                break
            kept.append(section)
        markdown = "\n".join(header) + "\n" + "\n\n".join(kept)

    html_doc = _render_html(results, problem, failures, suggested, missing)
    return Report(
        markdown=markdown, html=html_doc, passed=not failures, failures=failures
    )


_CSS = """
body{font-family:sans-serif;margin:2em;max-width:70em}
table{border-collapse:collapse}td,th{border:1px solid #ccc;padding:.3em .6em}
.grid span{display:inline-block;width:14px;height:14px;margin:1px;border-radius:2px}
.v-accepted{background:#2da44e}.v-wrong_answer{background:#cf222e}
.v-time_limit_exceeded{background:#bf8700}.v-run_time_error{background:#e16f24}
.v-compiler_error,.v-judge_error{background:#8250df}.v-not_run{background:#d0d7de}
.time-bar{display:inline-block;height:10px;background:#54aeff}
.limit{color:#cf222e}pre{background:#f6f8fa;padding:.6em;overflow-x:auto}
.fail{color:#cf222e}.ok{color:#2da44e}
"""


def _render_html(
    results: list[dict],
    problem: dict,
    failures: list[str],
    suggested: float | None,
    missing: list[dict],
) -> str:
    e = html.escape
    parts = [
        "<!doctype html><meta charset='utf-8'>",
        f"<title>{e(problem['name'])} — icpc-verify</title>",
        f"<style>{_CSS}</style>",
        f"<h1>{e(problem['name'])}</h1>",
        f"<p>시간제한 {problem['time_limit']:.3f}s · 메모리 {problem['memory_mib']}MiB"
        f" · validation {e(str(problem['validation']))}</p>",
    ]
    if suggested is not None:
        parts.append(f"<p>권장 시간제한: <b>{suggested:.1f}s</b></p>")
    if failures:
        items = "".join(f"<li>{e(f)}</li>" for f in failures)
        parts.append(f"<h2 class='fail'>실패 {len(failures)}건</h2><ul>{items}</ul>")
    else:
        parts.append("<h2 class='ok'>전부 기대대로입니다</h2>")

    for r in results:
        ok = r.get("expectation_met", False)
        parts.append(
            f"<h2>{'✅' if ok else '❌'} {e(r['rel_path'])}"
            f" <small>{e(r['language'])} · 기대 {e(r['expected'])}"
            f" · 실제 {e(r['verdict'])} · factor {r.get('machine_factor', 1):.2f}</small></h2>"
        )
        cells = "".join(
            f"<span class='v-{e(c['verdict'])}' title=\"{e(c['id'])}: {e(c['verdict'])}"
            f" wall {c['wall']:.3f}s cpu {c['cpu']:.3f}s mem {c['mem_kib']}KiB"
            f" exit {c['exit_code']}\"></span>"
            for c in r["testcases"]
        )
        parts.append(f"<div class='grid'>{cells}</div>")

        limit = r.get("time_limit", 0.0) or 0.0
        hard = r.get("hard_kill", 0.0) or 0.0
        scale = max(hard, limit, 0.001)
        bars = []
        for c in r["testcases"]:
            if c["verdict"] == verdicts.NOT_RUN:
                continue
            width = max(1, int(c["wall"] / scale * 300))
            bars.append(
                f"<div>{e(c['id'])} <span class='time-bar' style='width:{width}px'></span>"
                f" {c['wall']:.3f}s</div>"
            )
        if bars:
            limit_px = int(limit / scale * 300)
            parts.append(
                f"<p><small>기준선: <span class='limit'>|</span> 시간제한 {limit:.3f}s"
                f" (약 {limit_px}px 지점) · hard kill {hard:.3f}s (300px)</small></p>"
            )
            parts.extend(bars)

        for c in r["testcases"]:
            if c.get("expected_excerpt") or c.get("actual_excerpt"):
                parts.append(
                    f"<h3>{e(c['id'])} diff</h3>"
                    f"<b>expected</b><pre>{e(c['expected_excerpt'])}</pre>"
                    f"<b>actual</b><pre>{e(c['actual_excerpt'])}</pre>"
                )
            if c["message"] and c["verdict"] not in (verdicts.ACCEPTED, verdicts.NOT_RUN):
                parts.append(f"<pre>{e(c['message'][:4096])}</pre>")

        if r.get("compile_log") and r["verdict"] in (
            verdicts.COMPILER_ERROR, verdicts.JUDGE_ERROR
        ):
            parts.append(f"<h3>log</h3><pre>{e(r['compile_log'][:4096])}</pre>")

        for warning in r.get("warnings", []):
            parts.append(f"<p>⚠ {e(warning)}</p>")

    return "\n".join(parts)
```

- [ ] **Step 4: cli.py에 report 하위 명령을 추가한다**

```python
    report_p = sub.add_parser("report", help="result.json 들을 병합해 리포트를 만든다")
    report_p.add_argument("--results-dir", type=Path, required=True)
    report_p.add_argument("--problem", type=Path, required=True)
    report_p.add_argument("--summary-out", type=Path, required=True)
    report_p.add_argument("--html-out", type=Path, required=True)
    report_p.add_argument("--expected-matrix", type=Path, default=None)
    report_p.add_argument("--verdict-match", choices=["exact", "any-rejected"], default="exact")
```

```python
def run_report(args: argparse.Namespace) -> int:
    results = []
    for path in sorted(args.results_dir.rglob("result*.json")):
        results.append(json.loads(path.read_text(encoding="utf-8")))
    problem = json.loads(args.problem.read_text(encoding="utf-8"))
    if "problem" in problem:          # discover.json 전체를 넘겨도 동작한다
        problem = problem["problem"]
    expected_matrix = None
    if args.expected_matrix and args.expected_matrix.is_file():
        loaded = json.loads(args.expected_matrix.read_text(encoding="utf-8"))
        expected_matrix = loaded["matrix"] if "matrix" in loaded else loaded

    outcome = generate_report(
        results, problem, expected_matrix=expected_matrix, verdict_match=args.verdict_match
    )
    args.summary_out.parent.mkdir(parents=True, exist_ok=True)
    args.summary_out.write_text(outcome.markdown, encoding="utf-8")
    args.html_out.parent.mkdir(parents=True, exist_ok=True)
    args.html_out.write_text(outcome.html, encoding="utf-8")

    for failure in outcome.failures:
        print(f"FAIL: {failure}", file=sys.stderr)
    print(f"report: {len(results)}개 결과, {'통과' if outcome.passed else '실패'}")
    return EXIT_OK if outcome.passed else EXIT_MISMATCH
```

`main` dispatch에 `report` 분기를 추가한다.

- [ ] **Step 5: 테스트가 통과하는지 확인한다**

Run: `PYTHONPATH=src python3 -m pytest tests/unit/test_report.py -v && ruff check . && PYTHONPATH=src python3 -m pytest tests/unit -q`
Expected: report 테스트 13개 포함 전부 PASS

- [ ] **Step 6: 커밋**

```bash
git add src/icpc_verify/report.py src/icpc_verify/cli.py tests/unit/test_report.py
git commit -m "feat: generate job summary and html report from results"
```

---

### Task 4: composite action 3개와 reusable workflow

**Files:**
- Create: `discover/action.yml`
- Create: `judge/action.yml`
- Create: `report/action.yml`
- Create: `.github/workflows/verify.yml`

**Interfaces:**
- Consumes: Task 1·3의 CLI (`icpc-verify discover/judge/report`), `image/IMAGE_DIGEST`
- Produces: 사용자 workflow가 쓰는 진입점. spec §13의 input 이름 그대로.

action은 CI에서 selftest(Task 5)로만 검증된다. 이 task에서는 파일 작성 + `actionlint`
수준의 정적 확인(로컬에 있으면)까지만 하고, 동작 검증은 Task 5와 함께 CI에서 한다.

- [ ] **Step 1: discover/action.yml을 쓴다**

```yaml
name: icpc-verify discover
description: 채점 대상 solution 과 문제 설정을 결정한다
inputs:
  problem-dir:
    default: "."
  full:
    default: "false"
  solutions-filter:
    default: ""
  default-time-limit:
    default: "1"
  default-memory-mib:
    default: "2048"
outputs:
  matrix:
    value: ${{ steps.emit.outputs.matrix }}
  count:
    value: ${{ steps.emit.outputs.count }}
  full:
    value: ${{ steps.emit.outputs.full }}
  problem:
    value: ${{ steps.emit.outputs.problem }}
runs:
  using: composite
  steps:
    - uses: actions/setup-python@v5
      with:
        python-version: "3.12"
    - shell: bash
      run: python -m pip install --quiet "${{ github.action_path }}/.."
    - id: emit
      shell: bash
      env:
        FULL: ${{ inputs.full }}
        BEFORE: ${{ github.event.before || '' }}
        BASE_REF: ${{ github.base_ref || '' }}
      run: |
        set -eu
        args=(--problem-dir "${{ inputs.problem-dir }}" --output discover.json
              --event-name "${{ github.event_name }}"
              --head "${{ github.sha }}"
              --default-time-limit "${{ inputs.default-time-limit }}"
              --default-memory-mib "${{ inputs.default-memory-mib }}")
        if [ "$FULL" = "true" ]; then args+=(--full); fi
        if [ -n "$BEFORE" ]; then args+=(--before "$BEFORE"); fi
        if [ -n "$BASE_REF" ]; then args+=(--base-ref "$BASE_REF"); fi
        if [ -n "${{ inputs.solutions-filter }}" ]; then args+=(--solutions-filter "${{ inputs.solutions-filter }}"); fi
        icpc-verify discover "${args[@]}"
        {
          echo "matrix=$(jq -c '.matrix' discover.json)"
          echo "count=$(jq -r '.count' discover.json)"
          echo "full=$(jq -r '.full' discover.json)"
          echo "problem=$(jq -c '.problem' discover.json)"
        } >> "$GITHUB_OUTPUT"
    - shell: bash
      run: cp discover.json "$RUNNER_TEMP/discover.json"
```

(`set -eu` 아래서 `[ ... ] && cmd`를 마지막 줄로 쓰면 조건이 거짓일 때 step이 죽는다 —
그래서 위 YAML은 전부 `if` 문이다. 옮길 때 이 형태를 유지하라.)

- [ ] **Step 2: judge/action.yml을 쓴다**

```yaml
name: icpc-verify judge
description: solution 하나를 채점하고 결과 artifact 를 올린다
inputs:
  problem-dir:
    default: "."
  solution:
    required: true
  name:
    required: true
  judge-image:
    default: ""
  judge-all:
    default: "false"
  verdict-match:
    default: "exact"
  default-time-limit:
    default: "1"
  default-memory-mib:
    default: "2048"
  timelimit-overshoot:
    default: "2s|20%"
  output-limit-mib:
    default: "8"
  required-cpu-flags:
    default: "avx2"
  judge-cpu:
    default: ""
  offline-sibling:
    default: "true"
  diff-max-cases:
    default: "3"
  diff-max-bytes:
    default: "4096"
  compile-flags-cpp:
    default: ""
  compile-flags-c:
    default: ""
  compile-flags-java:
    default: ""
runs:
  using: composite
  steps:
    - uses: actions/setup-python@v5
      with:
        python-version: "3.12"
    - shell: bash
      run: python -m pip install --quiet "${{ github.action_path }}/.."
    - shell: bash
      run: |
        set -eu
        IMAGE="${{ inputs.judge-image }}"
        if [ -z "$IMAGE" ]; then
          IMAGE="$(cat "${{ github.action_path }}/../image/IMAGE_DIGEST")"
        fi
        echo "${{ github.token }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
        docker pull -q "$IMAGE"
        echo "JUDGE_IMAGE=$IMAGE" >> "$GITHUB_ENV"
    - shell: bash
      run: |
        set -eu
        args=(--problem-dir "${{ inputs.problem-dir }}"
              --solution "${{ inputs.solution }}"
              --output "result-${{ inputs.name }}.json"
              --image "$JUDGE_IMAGE"
              --verdict-match "${{ inputs.verdict-match }}"
              --default-time-limit "${{ inputs.default-time-limit }}"
              --default-memory-mib "${{ inputs.default-memory-mib }}"
              --timelimit-overshoot "${{ inputs.timelimit-overshoot }}"
              --output-limit-mib "${{ inputs.output-limit-mib }}"
              --required-cpu-flags "${{ inputs.required-cpu-flags }}"
              --diff-max-cases "${{ inputs.diff-max-cases }}"
              --diff-max-bytes "${{ inputs.diff-max-bytes }}")
        if [ "${{ inputs.judge-all }}" = "true" ]; then args+=(--judge-all); fi
        if [ -n "${{ inputs.judge-cpu }}" ]; then args+=(--judge-cpu "${{ inputs.judge-cpu }}"); fi
        if [ "${{ inputs.offline-sibling }}" != "true" ]; then args+=(--no-offline-sibling); fi
        if [ -n "${{ inputs.compile-flags-cpp }}" ]; then args+=(--compile-flags-cpp "${{ inputs.compile-flags-cpp }}"); fi
        if [ -n "${{ inputs.compile-flags-c }}" ]; then args+=(--compile-flags-c "${{ inputs.compile-flags-c }}"); fi
        if [ -n "${{ inputs.compile-flags-java }}" ]; then args+=(--compile-flags-java "${{ inputs.compile-flags-java }}"); fi
        icpc-verify judge "${args[@]}" || status=$?
        # exit 1(기대 불일치)은 result.json 이 있으므로 report 가 판정한다.
        # exit 2(설정/인프라 오류)만 job 실패로 만든다.
        status=${status:-0}
        if [ "$status" -ge 2 ]; then exit "$status"; fi
    - uses: actions/upload-artifact@v4
      with:
        name: icpc-verify-result-${{ inputs.name }}
        path: result-${{ inputs.name }}.json
        if-no-files-found: error
```

- [ ] **Step 3: report/action.yml을 쓴다**

```yaml
name: icpc-verify report
description: 결과를 병합해 Job Summary 와 HTML 리포트를 발행한다
inputs:
  problem:
    required: true
  expected-matrix:
    default: ""
  verdict-match:
    default: "exact"
outputs:
  passed:
    value: ${{ steps.gen.outputs.passed }}
runs:
  using: composite
  steps:
    - uses: actions/setup-python@v5
      with:
        python-version: "3.12"
    - shell: bash
      run: python -m pip install --quiet "${{ github.action_path }}/.."
    - uses: actions/download-artifact@v4
      with:
        pattern: icpc-verify-result-*
        path: results
    - id: gen
      shell: bash
      env:
        PROBLEM_JSON: ${{ inputs.problem }}
        MATRIX_JSON: ${{ inputs.expected-matrix }}
      run: |
        set -eu
        printf '%s' "$PROBLEM_JSON" > problem.json
        if [ -n "$MATRIX_JSON" ]; then
          printf '%s' "$MATRIX_JSON" > matrix.json
          extra=(--expected-matrix matrix.json)
        else
          extra=()
        fi
        status=0
        icpc-verify report --results-dir results --problem problem.json \
          --summary-out summary.md --html-out index.html \
          --verdict-match "${{ inputs.verdict-match }}" "${extra[@]}" || status=$?
        cat summary.md >> "$GITHUB_STEP_SUMMARY"
        if [ "$status" -eq 0 ]; then echo "passed=true" >> "$GITHUB_OUTPUT";
        else echo "passed=false" >> "$GITHUB_OUTPUT"; fi
        echo "status=$status" >> "$GITHUB_ENV"
    - uses: actions/upload-artifact@v4
      with:
        name: icpc-verify-report
        path: index.html
    - shell: bash
      run: exit "$status"
```

(input을 `'${{ ... }}'`로 직접 인용하면 작은따옴표가 든 값에서 깨진다 — 그래서 위 YAML은
`env:` 경유다. 옮길 때 이 형태를 유지하라.)

- [ ] **Step 4: .github/workflows/verify.yml을 쓴다**

```yaml
name: icpc-verify
on:
  workflow_call:
    inputs:
      full:
        type: boolean
        default: false
      runs-on:
        type: string
        default: ubuntu-latest
      problem-dir:
        type: string
        default: "."
      judge-image:
        type: string
        default: ""
      judge-all:
        type: boolean
        default: false
      verdict-match:
        type: string
        default: exact
      max-parallel:
        type: number
        default: 0
      solutions-filter:
        type: string
        default: ""
      default-time-limit:
        type: number
        default: 1
      default-memory-mib:
        type: number
        default: 2048
      timelimit-overshoot:
        type: string
        default: "2s|20%"
      output-limit-mib:
        type: number
        default: 8
      required-cpu-flags:
        type: string
        default: avx2
      judge-cpu:
        type: string
        default: ""
      offline-sibling:
        type: boolean
        default: true
      diff-max-cases:
        type: number
        default: 3
      diff-max-bytes:
        type: number
        default: 4096
      compile-flags-cpp:
        type: string
        default: ""
      compile-flags-c:
        type: string
        default: ""
      compile-flags-java:
        type: string
        default: ""

jobs:
  discover:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.d.outputs.matrix }}
      count: ${{ steps.d.outputs.count }}
      problem: ${{ steps.d.outputs.problem }}
      verifier-ref: ${{ steps.ref.outputs.ref }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - id: ref
        shell: bash
        run: |
          # github.workflow_ref 예: suckzoo/problem-verifier/.github/workflows/verify.yml@refs/heads/main
          ref="${{ github.workflow_ref }}"
          echo "ref=${ref##*@}" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@v4
        with:
          repository: suckzoo/problem-verifier
          ref: ${{ steps.ref.outputs.ref }}
          path: .icpc-verifier
      - id: d
        uses: ./.icpc-verifier/discover
        with:
          problem-dir: ${{ inputs.problem-dir }}
          full: ${{ inputs.full }}
          solutions-filter: ${{ inputs.solutions-filter }}
          default-time-limit: ${{ inputs.default-time-limit }}
          default-memory-mib: ${{ inputs.default-memory-mib }}

  judge:
    needs: discover
    if: needs.discover.outputs.count != '0'
    runs-on: ${{ inputs.runs-on }}
    strategy:
      fail-fast: false
      max-parallel: ${{ inputs.max-parallel > 0 && inputs.max-parallel || 256 }}
      matrix:
        include: ${{ fromJSON(needs.discover.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: suckzoo/problem-verifier
          ref: ${{ needs.discover.outputs.verifier-ref }}
          path: .icpc-verifier
      - uses: ./.icpc-verifier/judge
        with:
          problem-dir: ${{ inputs.problem-dir }}
          solution: ${{ matrix.path }}
          name: ${{ matrix.name }}
          judge-image: ${{ inputs.judge-image }}
          judge-all: ${{ inputs.judge-all }}
          verdict-match: ${{ inputs.verdict-match }}
          default-time-limit: ${{ inputs.default-time-limit }}
          default-memory-mib: ${{ inputs.default-memory-mib }}
          timelimit-overshoot: ${{ inputs.timelimit-overshoot }}
          output-limit-mib: ${{ inputs.output-limit-mib }}
          required-cpu-flags: ${{ inputs.required-cpu-flags }}
          judge-cpu: ${{ inputs.judge-cpu }}
          offline-sibling: ${{ inputs.offline-sibling }}
          diff-max-cases: ${{ inputs.diff-max-cases }}
          diff-max-bytes: ${{ inputs.diff-max-bytes }}
          compile-flags-cpp: ${{ inputs.compile-flags-cpp }}
          compile-flags-c: ${{ inputs.compile-flags-c }}
          compile-flags-java: ${{ inputs.compile-flags-java }}

  report:
    needs: [discover, judge]
    if: always() && needs.discover.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: suckzoo/problem-verifier
          ref: ${{ needs.discover.outputs.verifier-ref }}
          path: .icpc-verifier
      - uses: ./.icpc-verifier/report
        with:
          problem: ${{ needs.discover.outputs.problem }}
          expected-matrix: ${{ needs.discover.outputs.matrix }}
          verdict-match: ${{ inputs.verdict-match }}
```

주의:
- `max-parallel` 식은 `inputs.max-parallel`이 0일 때 256(사실상 무제한)이 되게 한다.
  GitHub 식 문법에서 `a && b || c` 패턴을 쓰되, 안 되면 두 job 분기 대신 기본값 256을
  input default로 바꾸는 것도 허용한다 (spec의 "0 = 제한 없음" 의미만 지키면 된다).
- `count`가 0이면 judge는 skip되고 report는 결과 0개로 돌아 "변경 없음"을 남기고 성공한다
  (expected-matrix도 빈 배열이므로 누락 검출이 발동하지 않는다).

- [ ] **Step 5: 문법 확인**

Run: `python3 -c "import yaml,glob; [yaml.safe_load(open(f)) for f in ['discover/action.yml','judge/action.yml','report/action.yml','.github/workflows/verify.yml']]; print('yaml ok')"`
Expected: `yaml ok`. `actionlint`가 로컬에 있으면 그것도 돌린다 (없으면 CI에 맡긴다).

- [ ] **Step 6: 커밋**

```bash
git add discover judge report .github/workflows/verify.yml
git commit -m "feat: add composite actions and reusable verify workflow"
```

---

### Task 5: selftest workflow와 fixture 보강

**Files:**
- Create: `tests/fixtures/float/problem.yaml`
- Create: `tests/fixtures/float/data/sample/01.in` `01.ans`
- Create: `tests/fixtures/float/data/secret/02.in` `02.ans`
- Create: `tests/fixtures/float/solutions/accepted/pi.py`
- Create: `tests/fixtures/float/solutions/wrong_answer/rough.py`
- Create: `tests/fixtures/mislabeled/problem.yaml`
- Create: `tests/fixtures/mislabeled/data/01.in` `01.ans`
- Create: `tests/fixtures/mislabeled/solutions/wrong_answer/actually_right.cpp`
- Create: `.github/workflows/selftest.yml`
- Modify: `README.md` (사용법을 reusable workflow 기준으로 갱신)

**Interfaces:**
- Consumes: Task 4의 `verify.yml`
- Produces: fixture 4 + 2개에 대한 end-to-end 검증. `mislabeled`는 **일부러 기대가 틀린**
  fixture로, workflow가 실패하는지(오탐 방어) 확인한다.

- [ ] **Step 1: float fixture를 만든다**

`tests/fixtures/float/problem.yaml`:

```yaml
problem_format_version: 2023-07-draft
name: Circle Area
type: pass-fail
limits:
  time_limit: 1.0
  memory: 512
validation: default
validator_flags: float_tolerance 1e-4
```

`data/sample/01.in`: `1\n` / `data/sample/01.ans`: `3.14159265\n`
`data/secret/02.in`: `2\n` / `data/secret/02.ans`: `12.56637061\n`

`solutions/accepted/pi.py` (ans와 자릿수가 다르지만 1e-4 안):

```python
import math

r = float(input())
print(f"{math.pi * r * r:.4f}")
```

`solutions/wrong_answer/rough.py` (오차 1e-4 밖):

```python
r = float(input())
print(3.14 * r * r)
```

- [ ] **Step 2: mislabeled fixture를 만든다**

`tests/fixtures/mislabeled/problem.yaml`:

```yaml
problem_format_version: 2023-07-draft
name: Mislabeled On Purpose
type: pass-fail
limits:
  time_limit: 1.0
  memory: 512
```

`data/01.in`: `1 2\n` / `data/01.ans`: `3\n`

`solutions/wrong_answer/actually_right.cpp` (정답인데 wrong_answer 디렉토리에 있다 —
verify가 이 불일치를 **잡아내서 실패해야** selftest가 통과):

```cpp
#include <cstdio>
int main() {
    long long a, b;
    if (scanf("%lld %lld", &a, &b) != 2) return 1;
    printf("%lld\n", a + b);
    return 0;
}
```

- [ ] **Step 3: 로컬에서 float fixture를 단위 수준으로 확인한다**

float fixture는 default validation이므로 순수 비교기로 검증 가능하다.
`tests/unit/test_float_fixture.py`:

```python
from pathlib import Path

from icpc_verify.compare import compare_output, parse_compare_flags
from icpc_verify.problemcfg import load_problem_config
from icpc_verify.testdata import collect_testcases

FIXTURE = Path(__file__).resolve().parents[2] / "tests" / "fixtures" / "float"


def test_fixture_parses_with_nested_data():
    config = load_problem_config(FIXTURE, default_time_limit=1.0, default_memory_mib=2048)
    assert config.validator_flags == ("float_tolerance", "1e-4")
    cases = collect_testcases(FIXTURE)
    assert [c.id for c in cases] == ["sample/01", "secret/02"]
    assert [c.group for c in cases] == ["sample", "secret"]


def test_accepted_output_within_tolerance():
    flags = parse_compare_flags(["float_tolerance", "1e-4"])
    ok, _ = compare_output(b"3.1416\n", b"3.14159265\n", flags)
    assert ok


def test_rough_output_outside_tolerance():
    flags = parse_compare_flags(["float_tolerance", "1e-4"])
    ok, _ = compare_output(b"3.14\n", b"3.14159265\n", flags)
    assert not ok
```

Run: `PYTHONPATH=src python3 -m pytest tests/unit/test_float_fixture.py -v`
Expected: PASS 3개

- [ ] **Step 4: selftest.yml을 쓴다**

`.github/workflows/selftest.yml`:

```yaml
name: selftest
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  packages: read

jobs:
  plain:
    uses: ./.github/workflows/verify.yml
    with:
      problem-dir: tests/fixtures/plain
      full: true
  float:
    uses: ./.github/workflows/verify.yml
    with:
      problem-dir: tests/fixtures/float
      full: true
  custom:
    uses: ./.github/workflows/verify.yml
    with:
      problem-dir: tests/fixtures/custom
      full: true
  interactive:
    uses: ./.github/workflows/verify.yml
    with:
      problem-dir: tests/fixtures/interactive
      full: true
  mislabeled:
    uses: ./.github/workflows/verify.yml
    with:
      problem-dir: tests/fixtures/mislabeled
      full: true

  assert:
    needs: [plain, float, custom, interactive, mislabeled]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        run: |
          set -eu
          fail=0
          check() {
            name=$1; got=$2; want=$3
            if [ "$got" != "$want" ]; then
              echo "::error::selftest $name: 결과 $got, 기대 $want"
              fail=1
            else
              echo "selftest $name: $got (기대대로)"
            fi
          }
          check plain       "${{ needs.plain.result }}"       success
          check float       "${{ needs.float.result }}"       success
          check custom      "${{ needs.custom.result }}"      success
          check interactive "${{ needs.interactive.result }}" success
          check mislabeled  "${{ needs.mislabeled.result }}"  failure
          exit "$fail"
```

주의: reusable workflow를 같은 repo에서 `uses: ./.github/workflows/verify.yml`로 부르면
`github.workflow_ref`는 **호출자(selftest.yml)의 ref**를 가리킨다 — verify.yml의 ref 파싱이
그 값으로 action repo를 checkout하므로 selftest에서는 자기 branch가 그대로 쓰인다. 즉 PR
branch의 action 변경이 selftest에 반영된다. 이것이 의도된 동작이다.

- [ ] **Step 5: README를 갱신한다**

`README.md`의 사용법 섹션을 reusable workflow 기준으로 바꾼다 (spec §3.1의 사용자 workflow
예시를 그대로). CLI 사용법은 "로컬 사용" 소절로 남긴다. 신뢰 모델 섹션은 유지한다.

- [ ] **Step 6: 로컬 검증 + 커밋 + push + CI/selftest 확인**

```bash
ruff check . && ruff format --check . && PYTHONPATH=src python3 -m pytest tests/unit -q
git add -A && git commit -m "feat: add selftest workflow with float and mislabeled fixtures"
git push -u origin <branch>
gh run list --repo Suckzoo/problem-verifier --branch <branch>
gh run watch <ci-run-id> --repo Suckzoo/problem-verifier --exit-status
gh run watch <selftest-run-id> --repo Suckzoo/problem-verifier --exit-status
```

Expected: ci(기존 unit+docker)와 selftest 둘 다 녹색. selftest의 assert job이
mislabeled=failure를 확인한다. **selftest는 pull_request 이벤트로도 돌므로 PR branch에서
바로 검증된다.** judge job들이 ghcr digest image를 pull하므로 packages: read 권한과
digest 유효성이 여기서 함께 검증된다.

---

## 계획 3 완료 조건

- [ ] `ruff check .` / `ruff format --check .` 통과
- [ ] 단위 테스트 전부 통과 (155 + Task 1: 15 + Task 2: 4 + Task 3: 13 + Task 5: 3 = 190 근방)
- [ ] CI docker-test 통과 (diff excerpt 테스트 포함)
- [ ] selftest workflow 녹색: plain/float/custom/interactive 성공, mislabeled 실패 확인
- [ ] Job Summary에 요약 표·이모지 띠·권장 시간제한이 보인다 (selftest 실행에서 육안 확인)
- [ ] `icpc-verify-report` artifact의 index.html이 단일 파일로 열린다

## 남는 것 (범위 밖, 기록)

- spec §7의 sibling offline은 hosted 2 vCPU에서 best-effort로 동작한다 (계획 1 구현 그대로).
- validator 빌드가 matrix job마다 1회씩 일어난다 — job이 서로 다른 VM이므로 구조적으로 불가피.
- `test_oom_is_detected` flake는 독립 이슈로 남아 있다 (계획 2 최종 리뷰 참조).
- interactive의 신뢰 모델 제약(README/spec 명문화됨)은 report에도 적용된다: judgemessage는
  escape되어 표시된다 (Task 3에서 구현).
